/**
 * Mock for resources/skins.citizen.preferences/overrides.json
 *
 * Mutable: tests can modify .overrides and .messages between runs
 * because require() returns the same object reference.
 */
module.exports = { overrides: null, messages: {} };
