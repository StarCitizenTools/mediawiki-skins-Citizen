/**
 * Stub for App.vue used by init.js tests.
 *
 * init.js only passes the component to Vue.createMwApp(), which tests
 * mock anyway, so a minimal object suffices.
 */
module.exports = { name: 'AppStub', template: '<div />' };
